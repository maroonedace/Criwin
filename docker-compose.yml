# Shared by every service that talks to the database.
x-db-env: &db-env
  DB_HOST: ${DB_HOST}
  DB_PORT: ${DB_PORT}
  DB_NAME: ${DB_NAME}
  DB_USER: ${DB_USER}
  DB_PASSWORD: ${DB_PASSWORD}

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  storage:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${STORAGE_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${STORAGE_SECRET_KEY}
    volumes:
      - storage_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  # One-shot: brings the schema to head, then exits. app and admin wait for it to
  # succeed, so a failed migration stops the stack rather than letting the bot run
  # against a schema it doesn't expect. It carries its own build block so
  # `docker compose run --rm migrate` works on a host that has never built the image.
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
    image: criwin
    restart: "no"
    depends_on:
      db:
        condition: service_healthy
    command: ["python", "-m", "src.services.db.migrate"]
    environment:
      <<: *db-env
      LOG_LEVEL: ${LOG_LEVEL}

  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: criwin
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      storage:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    environment:
      ENVIRONMENT: ${ENVIRONMENT}
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      GUILD_ID: ${GUILD_ID}
      LOG_LEVEL: ${LOG_LEVEL}

      # Database Configuration
      <<: *db-env

      # Storage Configuration
      STORAGE_REGION: ${STORAGE_REGION}
      STORAGE_ACCESS_KEY: ${STORAGE_ACCESS_KEY}
      STORAGE_SECRET_KEY: ${STORAGE_SECRET_KEY}
      STORAGE_ENDPOINT: ${STORAGE_ENDPOINT}
      STORAGE_BUCKET_NAME: ${STORAGE_BUCKET_NAME}
      STORAGE_SECURE: ${STORAGE_SECURE}
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    volumes:
      - criwin_cache:/criwin/cache
      - criwin_cookies:/criwin/cookies

  admin:
    image: criwin
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      storage:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    command: ["python", "-m", "src.web"]
    environment:
      LOG_LEVEL: ${LOG_LEVEL}

      # Database Configuration
      <<: *db-env

      # Storage Configuration
      STORAGE_REGION: ${STORAGE_REGION}
      STORAGE_ACCESS_KEY: ${STORAGE_ACCESS_KEY}
      STORAGE_SECRET_KEY: ${STORAGE_SECRET_KEY}
      STORAGE_ENDPOINT: ${STORAGE_ENDPOINT}
      STORAGE_BUCKET_NAME: ${STORAGE_BUCKET_NAME}
      STORAGE_SECURE: ${STORAGE_SECURE}

      ADMIN_HOST: ${ADMIN_HOST}
      ADMIN_USERNAME: ${ADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      ADMIN_PORT: ${ADMIN_PORT}
    ports:
      - "${ADMIN_PUBLISH_HOST}:${ADMIN_PORT}:${ADMIN_PORT}"

volumes:
  db_data:
  storage_data:
  criwin_cache:
  criwin_cookies:
